# SF brands: plan

Spec: [2026-09-13-sf-brands-design.md](../specs/2026-09-13-sf-brands-design.md).
Worktree `../larp-brands`, branch `sf-brands`, off `origin/main` (b790ced).

1. Baseline: measure frame cost (max zoom-out, 1, 2) on `origin/main` with a local server on its own ports; save before-screenshots of downtown, SoMa, the Embarcadero, and the freeway.
2. Roster: add `game/art/brands.py` with all 35 brands; move the existing 15 into it with custom draw hooks so their art is unchanged (diff the regenerated PNGs against the current ones).
3. Art: the mark library and shared layouts in `make_ads.py`, driven by `BRANDS`; add the wall board, name band, and 1x1/1x2 storefronts and HQs in `lib/signs.py` and `lib/archetypes.py`; roster tests (art coverage, sizes, contrast).
4. Catalog: `catalog.py` builds branded entries from `BRANDS`; V boards become freeway props.
5. Palette: 24 sign slots, 52 day colors; full SF render, pixelize with `--new-palette`, `check_register.py`, contact sheets at 1x and 4x reviewed and fixed until every sign reads.
6. Engine: `areas` in the city definition; branded-first placement in `lots.ts` / `sprite-pick.ts`; freeway V-board placement beside the highway ring; placement tests.
7. Checks: game build and tests, server tests and tsc with a local database, art tests; fix any flaky test found.
8. Browser: the four areas, day and night, at 0.72, 1, and 4; name every new company; fix what does not read; after-measurements.
9. Docs: `game/README.md` (Building sprites), progress doc with deviations and the performance table.
10. Rebase on `origin/main`, open the PR, and ask before merging.
